---
bootstrap:
  dcs:
    postgresql:
      parameters:
        archive_mode: on
        archive_command: '/opt/cpm/bin/wrapper/pgbackrest archive-push "%p"'
      use_slots: false
      recovery_conf:
        restore_command: '/opt/cpm/bin/wrapper/pgbackrest archive-get %f "%p"'
